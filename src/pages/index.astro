---
export const prerender = true;

import Home from "../layouts/Home.astro";
import { getDict } from "../i18n";

const locale = "en" as const;
const dict = getDict(locale);
---

<Home dict={dict} locale={locale} />
